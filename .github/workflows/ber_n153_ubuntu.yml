name: Ubuntu N153
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/base.yml
    with:
      compiler: "ocaml-variants.5.3.0+BER"
      os: "ubuntu-latest"
